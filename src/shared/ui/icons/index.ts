/**
 * Icon proxy. Always import icons via `@icons` (or `@ui/icons`) so we can swap
 * the underlying icon library in one place.
 *
 * Usage:
 *   import { Activity, Stethoscope } from '@icons';
 */
export * from 'lucide-react';
